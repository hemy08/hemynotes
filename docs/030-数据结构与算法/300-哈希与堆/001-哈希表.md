# 哈希表

## 概述

哈希表是根据键直接访问值的数据结构，通过哈希函数将键映射到数组索引，实现O(1)平均时间复杂度的查找、插入和删除。

## 哈希表特点

1. **快速访问**：平均O(1)时间复杂度
2. **键值映射**：键到值的映射关系
3. **冲突处理**：处理不同键映射到相同位置
4. **动态扩容**：负载因子过高时自动扩容

## 核心概念

### 哈希函数

```c
unsigned int hash(int key, int size) {
    return key % size;
}

unsigned int hashString(const char *str, int size) {
    unsigned int hash = 5381;
    int c;
    while ((c = *str++)) {
        hash = ((hash << 5) + hash) + c;
    }
    return hash % size;
}
```

### 常用哈希函数

| 哈希函数 | 特点 | 应用 |
|---------|------|------|
| 取模法 | h(k) = k mod m | 整数键 |
| 乘数法 | h(k) = ⌊m(kA mod 1)⌋ | 分布均匀 |
| 平方取中 | 取k²的中间几位 | 减少冲突 |
| FNV | 快速散列 | 字符串 |

### 负载因子

```
负载因子 α = n / m
n: 已存储元素数量
m: 哈希表大小

建议: α ≤ 0.75
```

## 冲突解决方法

### 链地址法

```c
typedef struct HashNode {
    int key;
    int value;
    struct HashNode *next;
} HashNode;

typedef struct {
    HashNode **buckets;
    int size;
    int count;
} HashMap;

HashMap* createHashMap(int size) {
    HashMap *map = (HashMap*)malloc(sizeof(HashMap));
    map->size = size;
    map->count = 0;
    map->buckets = (HashNode**)calloc(size, sizeof(HashNode*));
    return map;
}

void insert(HashMap *map, int key, int value) {
    int index = hash(key, map->size);
    
    HashNode *curr = map->buckets[index];
    while (curr != NULL) {
        if (curr->key == key) {
            curr->value = value;
            return;
        }
        curr = curr->next;
    }
    
    HashNode *newNode = (HashNode*)malloc(sizeof(HashNode));
    newNode->key = key;
    newNode->value = value;
    newNode->next = map->buckets[index];
    map->buckets[index] = newNode;
    map->count++;
}

int get(HashMap *map, int key, int *found) {
    int index = hash(key, map->size);
    HashNode *curr = map->buckets[index];
    
    while (curr != NULL) {
        if (curr->key == key) {
            *found = 1;
            return curr->value;
        }
        curr = curr->next;
    }
    
    *found = 0;
    return -1;
}

void remove(HashMap *map, int key) {
    int index = hash(key, map->size);
    HashNode *curr = map->buckets[index];
    HashNode *prev = NULL;
    
    while (curr != NULL) {
        if (curr->key == key) {
            if (prev == NULL) {
                map->buckets[index] = curr->next;
            } else {
                prev->next = curr->next;
            }
            free(curr);
            map->count--;
            return;
        }
        prev = curr;
        curr = curr->next;
    }
}
```

### 开放地址法

```c
typedef struct {
    int *keys;
    int *values;
    int *occupied;
    int size;
    int count;
} OpenHashMap;

OpenHashMap* createOpenHashMap(int size) {
    OpenHashMap *map = (OpenHashMap*)malloc(sizeof(OpenHashMap));
    map->size = size;
    map->count = 0;
    map->keys = (int*)calloc(size, sizeof(int));
    map->values = (int*)calloc(size, sizeof(int));
    map->occupied = (int*)calloc(size, sizeof(int));
    return map;
}

int probe(OpenHashMap *map, int key) {
    int index = hash(key, map->size);
    int i = 0;
    
    while (map->occupied[index] && map->keys[index] != key) {
        i++;
        index = (index + i * i) % map->size;
    }
    
    return index;
}

void insertOpen(OpenHashMap *map, int key, int value) {
    if (map->count >= map->size * 0.75) {
        return;
    }
    
    int index = probe(map, key);
    
    if (!map->occupied[index]) {
        map->count++;
    }
    
    map->keys[index] = key;
    map->values[index] = value;
    map->occupied[index] = 1;
}

int getOpen(OpenHashMap *map, int key, int *found) {
    int index = probe(map, key);
    
    if (map->occupied[index] && map->keys[index] == key) {
        *found = 1;
        return map->values[index];
    }
    
    *found = 0;
    return -1;
}
```

## 动态扩容

```c
void resize(HashMap *map, int newSize) {
    HashNode **oldBuckets = map->buckets;
    int oldSize = map->size;
    
    map->buckets = (HashNode**)calloc(newSize, sizeof(HashNode*));
    map->size = newSize;
    map->count = 0;
    
    for (int i = 0; i < oldSize; i++) {
        HashNode *curr = oldBuckets[i];
        while (curr != NULL) {
            HashNode *next = curr->next;
            insert(map, curr->key, curr->value);
            free(curr);
            curr = next;
        }
    }
    
    free(oldBuckets);
}

void checkResize(HashMap *map) {
    double loadFactor = (double)map->count / map->size;
    if (loadFactor > 0.75) {
        resize(map, map->size * 2);
    }
}
```

## C++ 实现

```cpp
template<typename K, typename V>
class HashMap {
private:
    struct Node {
        K key;
        V value;
        Node *next;
        Node(const K& k, const V& v) : key(k), value(v), next(nullptr) {}
    };
    
    std::vector<Node*> buckets;
    int count;
    
    size_t hash(const K& key) {
        return std::hash<K>{}(key) % buckets.size();
    }
    
public:
    HashMap(int size = 16) : buckets(size, nullptr), count(0) {}
    
    void insert(const K& key, const V& value) {
        size_t index = hash(key);
        Node *curr = buckets[index];
        
        while (curr) {
            if (curr->key == key) {
                curr->value = value;
                return;
            }
            curr = curr->next;
        }
        
        Node *newNode = new Node(key, value);
        newNode->next = buckets[index];
        buckets[index] = newNode;
        count++;
    }
    
    V* get(const K& key) {
        size_t index = hash(key);
        Node *curr = buckets[index];
        
        while (curr) {
            if (curr->key == key) return &curr->value;
            curr = curr->next;
        }
        return nullptr;
    }
    
    bool remove(const K& key) {
        size_t index = hash(key);
        Node *curr = buckets[index];
        Node *prev = nullptr;
        
        while (curr) {
            if (curr->key == key) {
                if (prev) prev->next = curr->next;
                else buckets[index] = curr->next;
                delete curr;
                count--;
                return true;
            }
            prev = curr;
            curr = curr->next;
        }
        return false;
    }
    
    bool contains(const K& key) {
        return get(key) != nullptr;
    }
    
    int size() { return count; }
};
```

## STL使用

```cpp
#include <unordered_map>
#include <unordered_set>

std::unordered_map<std::string, int> map;
map["apple"] = 1;
map["banana"] = 2;

if (map.find("apple") != map.end()) {
    std::cout << map["apple"] << std::endl;
}

map.erase("apple");

std::unordered_set<int> set;
set.insert(1);
set.insert(2);
set.insert(3);

if (set.count(2)) {
    std::cout << "Found" << std::endl;
}
```

## 时间复杂度

| 操作 | 平均 | 最坏 |
|------|------|------|
| 查找 | O(1) | O(n) |
| 插入 | O(1) | O(n) |
| 删除 | O(1) | O(n) |

## 哈希表问题

### 两数之和

```c
int* twoSum(int nums[], int n, int target, int *returnSize) {
    HashMap *map = createHashMap(n);
    int *result = (int*)malloc(2 * sizeof(int));
    *returnSize = 2;
    
    for (int i = 0; i < n; i++) {
        int complement = target - nums[i];
        int found;
        int index = get(map, complement, &found);
        
        if (found) {
            result[0] = index;
            result[1] = i;
            return result;
        }
        
        insert(map, nums[i], i);
    }
    
    return result;
}
```

### 字符统计

```c
void charCount(const char *str) {
    int count[256] = {0};
    
    for (int i = 0; str[i]; i++) {
        count[(unsigned char)str[i]]++;
    }
    
    for (int i = 0; i < 256; i++) {
        if (count[i] > 0) {
            printf("%c: %d\n", i, count[i]);
        }
    }
}
```

### LRU缓存

```cpp
class LRUCache {
private:
    int capacity;
    std::list<std::pair<int, int>> cache;
    std::unordered_map<int, std::list<std::pair<int, int>>::iterator> map;
    
public:
    LRUCache(int cap) : capacity(cap) {}
    
    int get(int key) {
        if (map.find(key) == map.end()) return -1;
        cache.splice(cache.begin(), cache, map[key]);
        return map[key]->second;
    }
    
    void put(int key, int value) {
        if (map.find(key) != map.end()) {
            map[key]->second = value;
            cache.splice(cache.begin(), cache, map[key]);
            return;
        }
        
        if (cache.size() == capacity) {
            int oldKey = cache.back().first;
            cache.pop_back();
            map.erase(oldKey);
        }
        
        cache.push_front({key, value});
        map[key] = cache.begin();
    }
};
```

## 冲突解决方法比较

| 方法 | 优点 | 缺点 |
|------|------|------|
| 链地址法 | 实现简单，删除方便 | 额外指针开销 |
| 线性探测 | 缓存友好 | 聚集现象 |
| 二次探测 | 减少聚集 | 可能不遍历全表 |
| 双重哈希 | 分布均匀 | 计算两次哈希 |

## 应用场景

1. **符号表**：编译器变量管理
2. **缓存**：Redis、Memcached
3. **去重**：数据去重
4. **计数**：频率统计
5. **索引**：数据库索引

## 参考资料

- 《算法导论》第11章
- 《数据结构与算法分析》第5章
