# AVL树

## 概述

AVL树是最早发明的自平衡二叉搜索树，由Adelson-Velsky和Landis在1962年提出。任意节点的左右子树高度差不超过1。

## AVL树特点

1. **严格平衡**：|height(left) - height(right)| ≤ 1
2. **平衡因子**：节点平衡因子 = 左子树高度 - 右子树高度
3. **查找高效**：查找、插入、删除均为O(log n)
4. **旋转调整**：通过旋转维持平衡

## 平衡因子

平衡因子取值范围：{-1, 0, 1}

| 平衡因子 | 状态 |
|---------|------|
| -1 | 右子树较高 |
| 0 | 左右子树等高 |
| 1 | 左子树较高 |
| 其他 | 不平衡，需调整 |

## 时间复杂度

| 操作 | 时间复杂度 |
|------|-----------|
| 查找 | O(log n) |
| 插入 | O(log n) |
| 删除 | O(log n) |
| 旋转 | O(1) |

## AVL树实现

### 节点定义

```c
typedef struct AVLNode {
    int data;
    int height;
    struct AVLNode *left;
    struct AVLNode *right;
} AVLNode;

int height(AVLNode *node) {
    if (node == NULL) return 0;
    return node->height;
}

int max(int a, int b) {
    return a > b ? a : b;
}

int getBalance(AVLNode *node) {
    if (node == NULL) return 0;
    return height(node->left) - height(node->right);
}

AVLNode* createNode(int data) {
    AVLNode *node = (AVLNode*)malloc(sizeof(AVLNode));
    node->data = data;
    node->height = 1;
    node->left = NULL;
    node->right = NULL;
    return node;
}

void updateHeight(AVLNode *node) {
    node->height = 1 + max(height(node->left), height(node->right));
}
```

### 右旋（LL型）

```c
AVLNode* rightRotate(AVLNode *y) {
    AVLNode *x = y->left;
    AVLNode *T2 = x->right;
    
    x->right = y;
    y->left = T2;
    
    updateHeight(y);
    updateHeight(x);
    
    return x;
}
```

### 左旋（RR型）

```c
AVLNode* leftRotate(AVLNode *x) {
    AVLNode *y = x->right;
    AVLNode *T2 = y->left;
    
    y->left = x;
    x->right = T2;
    
    updateHeight(x);
    updateHeight(y);
    
    return y;
}
```

### 双旋转

```c
AVLNode* balance(AVLNode *node) {
    updateHeight(node);
    int bf = getBalance(node);
    
    if (bf > 1) {
        if (getBalance(node->left) < 0) {
            node->left = leftRotate(node->left);
        }
        return rightRotate(node);
    }
    
    if (bf < -1) {
        if (getBalance(node->right) > 0) {
            node->right = rightRotate(node->right);
        }
        return leftRotate(node);
    }
    
    return node;
}
```

### 插入操作

```c
AVLNode* insert(AVLNode *node, int data) {
    if (node == NULL) return createNode(data);
    
    if (data < node->data) {
        node->left = insert(node->left, data);
    } else if (data > node->data) {
        node->right = insert(node->right, data);
    } else {
        return node;
    }
    
    return balance(node);
}
```

### 删除操作

```c
AVLNode* findMin(AVLNode *node) {
    while (node->left != NULL) {
        node = node->left;
    }
    return node;
}

AVLNode* delete(AVLNode *root, int data) {
    if (root == NULL) return NULL;
    
    if (data < root->data) {
        root->left = delete(root->left, data);
    } else if (data > root->data) {
        root->right = delete(root->right, data);
    } else {
        if (root->left == NULL || root->right == NULL) {
            AVLNode *temp = root->left ? root->left : root->right;
            if (temp == NULL) {
                temp = root;
                root = NULL;
            } else {
                *root = *temp;
            }
            free(temp);
        } else {
            AVLNode *temp = findMin(root->right);
            root->data = temp->data;
            root->right = delete(root->right, temp->data);
        }
    }
    
    if (root == NULL) return NULL;
    
    return balance(root);
}
```

### 查找操作

```c
AVLNode* search(AVLNode *root, int data) {
    if (root == NULL || root->data == data) {
        return root;
    }
    
    if (data < root->data) {
        return search(root->left, data);
    }
    return search(root->right, data);
}
```

## C++ 实现

```cpp
template<typename T>
class AVLTree {
private:
    struct Node {
        T data;
        int height;
        Node *left, *right;
        Node(T val) : data(val), height(1), left(nullptr), right(nullptr) {}
    };
    
    Node *root;
    
    int height(Node *node) { return node ? node->height : 0; }
    int balanceFactor(Node *node) { return height(node->left) - height(node->right); }
    
    void updateHeight(Node *node) {
        node->height = 1 + std::max(height(node->left), height(node->right));
    }
    
    Node* rotateRight(Node *y) {
        Node *x = y->left;
        y->left = x->right;
        x->right = y;
        updateHeight(y);
        updateHeight(x);
        return x;
    }
    
    Node* rotateLeft(Node *x) {
        Node *y = x->right;
        x->right = y->left;
        y->left = x;
        updateHeight(x);
        updateHeight(y);
        return y;
    }
    
    Node* balance(Node *node) {
        updateHeight(node);
        int bf = balanceFactor(node);
        
        if (bf > 1) {
            if (balanceFactor(node->left) < 0)
                node->left = rotateLeft(node->left);
            return rotateRight(node);
        }
        if (bf < -1) {
            if (balanceFactor(node->right) > 0)
                node->right = rotateRight(node->right);
            return rotateLeft(node);
        }
        return node;
    }
    
    Node* insert(Node *node, T data) {
        if (!node) return new Node(data);
        if (data < node->data) node->left = insert(node->left, data);
        else if (data > node->data) node->right = insert(node->right, data);
        else return node;
        return balance(node);
    }
    
public:
    AVLTree() : root(nullptr) {}
    void insert(T data) { root = insert(root, data); }
};
```

## 四种旋转情况

### LL型（左左）

```
    z                y
   / \              / \
  y   T3   =>      x   z
 / \                  / \
x   T2               T2  T3
```

### RR型（右右）

```
  x                  y
 / \                / \
T1  y     =>       x   z
   / \            / \
  T2  z          T1  T2
```

### LR型（左右）

```
    z               z              x
   / \             / \            / \
  y   T4  =>      x   T4   =>    y   z
 / \             / \            / \   \
T1  x           y   T3         T1  T2  T3
   / \         / \
  T2  T3      T1  T2
```

### RL型（右左）

```
  x             x                  y
 / \           / \                / \
T1  z   =>    T1  y      =>      x   z
   / \            / \          / \   \
  y   T4         T2  z        T1  T2  T3
 / \                / \
T2  T3             T3  T4
```

## AVL树验证

```c
int isAVL(AVLNode *root) {
    if (root == NULL) return 1;
    
    int bf = getBalance(root);
    if (bf < -1 || bf > 1) return 0;
    
    return isAVL(root->left) && isAVL(root->right);
}
```

## AVL树性质

1. 高度h满足：h ≤ 1.44 * log₂(n + 2)
2. 最少节点数：n(h) = n(h-1) + n(h-2) + 1
3. 查找时间上界：O(log n)
4. 旋转次数：插入最多1次，删除最多O(log n)次

## AVL vs 红黑树

| 特性 | AVL树 | 红黑树 |
|------|-------|--------|
| 平衡程度 | 严格平衡 | 近似平衡 |
| 查找效率 | 更优 | 稍差 |
| 插入删除 | 更多旋转 | 较少旋转 |
| 空间开销 | 存储高度 | 存储颜色 |
| 适用场景 | 查找密集 | 插入删除频繁 |

## 应用场景

1. **数据库索引**：需要高效查找
2. **内存数据库**：频繁查询场景
3. **符号表**：编译器实现
4. **事件调度**：定时器管理

## 参考资料

- 《算法导论》第12章
- Adelson-Velsky, Landis (1962) 原始论文
